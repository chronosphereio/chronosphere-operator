---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: chronospheremonitors.monitoring.chronosphere.io
spec:
  group: monitoring.chronosphere.io
  names:
    kind: ChronosphereMonitor
    listKind: ChronosphereMonitorList
    plural: chronospheremonitors
    singular: chronospheremonitor
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChronosphereMonitor is the Schema for the chronospheremonitors
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              Configv1Monitor configv1 monitor


              swagger:model configv1Monitor
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: |-
                  Annotations are visible in notifications generated by this monitor.
                  They can be be templated with labels from notifications.
                type: object
              bucket_slug:
                description: |-
                  Slug of the bucket the monitor belongs to. Required if collection_slug is
                  not set.
                type: string
              collection:
                description: collection
                properties:
                  slug:
                    description: slug
                    type: string
                  type:
                    description: type
                    type: string
                type: object
              collection_slug:
                description: |-
                  Slug of the collection the monitor belongs to. Required if bucket_slug is
                  not set.
                type: string
              created_at:
                description: |-
                  Timestamp of when the Monitor was created. Cannot be set by clients.
                  Read Only: true
                type: string
              graphite_query:
                description: |-
                  Graphite query. If set, no other queries can be set.
                  Example: sumSeries(stats.timers.*.mean_90)
                type: string
              interval_secs:
                description: |-
                  Optional setting for configuring how often alerts are evaluated.
                  If this is not set, it will default to 60s.
                format: int32
                type: integer
              labels:
                additionalProperties:
                  type: string
                description: |-
                  Labels are visible in notifications generated by this monitor,
                  and can be used to route alerts with notification overrides.
                type: object
              name:
                description: Required name of the Monitor. May be modified after the
                  Monitor is created.
                type: string
              notification_policy_slug:
                description: |-
                  Optional notification policy used to route alerts generated by the monitor.
                  If omitted, the notification policy is inherited from the monitor's bucket.
                type: string
              prometheus_query:
                description: |-
                  PromQL query. If set, no other queries can be set.
                  Example: up{job=\"prometheus\"} == 0
                type: string
              schedule:
                description: schedule
                properties:
                  timezone:
                    description: The timezone of the time ranges.
                    type: string
                  weekly_schedule:
                    description: weekly schedule
                    properties:
                      friday:
                        description: friday
                        properties:
                          active:
                            description: active
                            type: string
                          ranges:
                            description: |-
                              The time ranges that the monitor is active on this day. Required if
                              active is set to ONLY_DURING_RANGES, else must be empty.
                            items:
                              description: |-
                                ScheduleDayTimeRange schedule day time range


                                swagger:model ScheduleDayTimeRange
                              properties:
                                end_hh_mm:
                                  description: End time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                                start_hh_mm:
                                  description: Start time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                              type: object
                            type: array
                        type: object
                      monday:
                        description: monday
                        properties:
                          active:
                            description: active
                            type: string
                          ranges:
                            description: |-
                              The time ranges that the monitor is active on this day. Required if
                              active is set to ONLY_DURING_RANGES, else must be empty.
                            items:
                              description: |-
                                ScheduleDayTimeRange schedule day time range


                                swagger:model ScheduleDayTimeRange
                              properties:
                                end_hh_mm:
                                  description: End time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                                start_hh_mm:
                                  description: Start time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                              type: object
                            type: array
                        type: object
                      saturday:
                        description: saturday
                        properties:
                          active:
                            description: active
                            type: string
                          ranges:
                            description: |-
                              The time ranges that the monitor is active on this day. Required if
                              active is set to ONLY_DURING_RANGES, else must be empty.
                            items:
                              description: |-
                                ScheduleDayTimeRange schedule day time range


                                swagger:model ScheduleDayTimeRange
                              properties:
                                end_hh_mm:
                                  description: End time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                                start_hh_mm:
                                  description: Start time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                              type: object
                            type: array
                        type: object
                      sunday:
                        description: sunday
                        properties:
                          active:
                            description: active
                            type: string
                          ranges:
                            description: |-
                              The time ranges that the monitor is active on this day. Required if
                              active is set to ONLY_DURING_RANGES, else must be empty.
                            items:
                              description: |-
                                ScheduleDayTimeRange schedule day time range


                                swagger:model ScheduleDayTimeRange
                              properties:
                                end_hh_mm:
                                  description: End time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                                start_hh_mm:
                                  description: Start time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                              type: object
                            type: array
                        type: object
                      thursday:
                        description: thursday
                        properties:
                          active:
                            description: active
                            type: string
                          ranges:
                            description: |-
                              The time ranges that the monitor is active on this day. Required if
                              active is set to ONLY_DURING_RANGES, else must be empty.
                            items:
                              description: |-
                                ScheduleDayTimeRange schedule day time range


                                swagger:model ScheduleDayTimeRange
                              properties:
                                end_hh_mm:
                                  description: End time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                                start_hh_mm:
                                  description: Start time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                              type: object
                            type: array
                        type: object
                      tuesday:
                        description: tuesday
                        properties:
                          active:
                            description: active
                            type: string
                          ranges:
                            description: |-
                              The time ranges that the monitor is active on this day. Required if
                              active is set to ONLY_DURING_RANGES, else must be empty.
                            items:
                              description: |-
                                ScheduleDayTimeRange schedule day time range


                                swagger:model ScheduleDayTimeRange
                              properties:
                                end_hh_mm:
                                  description: End time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                                start_hh_mm:
                                  description: Start time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                              type: object
                            type: array
                        type: object
                      wednesday:
                        description: wednesday
                        properties:
                          active:
                            description: active
                            type: string
                          ranges:
                            description: |-
                              The time ranges that the monitor is active on this day. Required if
                              active is set to ONLY_DURING_RANGES, else must be empty.
                            items:
                              description: |-
                                ScheduleDayTimeRange schedule day time range


                                swagger:model ScheduleDayTimeRange
                              properties:
                                end_hh_mm:
                                  description: End time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                                start_hh_mm:
                                  description: Start time in the in format "<hour>:<minute>",
                                    e.g. "15:30".
                                  type: string
                              type: object
                            type: array
                        type: object
                    type: object
                type: object
              series_conditions:
                description: series conditions
                properties:
                  defaults:
                    description: defaults
                    properties:
                      critical:
                        description: critical
                        properties:
                          conditions:
                            description: |-
                              List of conditions to evaluate against a series.
                              Only one condition -- not all -- needs to match in order to assign a severity to a signal.
                            items:
                              description: |-
                                MonitorCondition monitor condition


                                swagger:model MonitorCondition
                              properties:
                                op:
                                  description: op
                                  type: string
                                resolve_sustain_secs:
                                  description: Time op operation must evaluate as
                                    false for an active alert, before that alert resolves.
                                  format: int32
                                  type: integer
                                sustain_secs:
                                  description: Time op operation must evaluate to
                                    true for the condition to evaluate to true.
                                  format: int32
                                  type: integer
                                value:
                                  description: The value to compare to the metric
                                    value using the op operation.
                                  type: number
                              type: object
                            type: array
                        type: object
                      warn:
                        description: warn
                        properties:
                          conditions:
                            description: |-
                              List of conditions to evaluate against a series.
                              Only one condition -- not all -- needs to match in order to assign a severity to a signal.
                            items:
                              description: |-
                                MonitorCondition monitor condition


                                swagger:model MonitorCondition
                              properties:
                                op:
                                  description: op
                                  type: string
                                resolve_sustain_secs:
                                  description: Time op operation must evaluate as
                                    false for an active alert, before that alert resolves.
                                  format: int32
                                  type: integer
                                sustain_secs:
                                  description: Time op operation must evaluate to
                                    true for the condition to evaluate to true.
                                  format: int32
                                  type: integer
                                value:
                                  description: The value to compare to the metric
                                    value using the op operation.
                                  type: number
                              type: object
                            type: array
                        type: object
                    type: object
                  overrides:
                    description: |-
                      Optional list of overrides to use for series having matching labels.
                      Each override defines labels that potentially match a series' labels.
                      If one or more overrides match a series, the severity conditions of the first matching
                      override are used instead of the defaults.


                      Cannot be used if graphite_query is set.
                    items:
                      description: |-
                        MonitorSeriesConditionsOverride monitor series conditions override


                        swagger:model MonitorSeriesConditionsOverride
                      properties:
                        label_matchers:
                          description: |-
                            Set of matchers on a series' labels.
                            If all labels match then the conditions defined in this override are used.
                          items:
                            description: |-
                              Configv1LabelMatcher configv1 label matcher


                              swagger:model configv1LabelMatcher
                            properties:
                              name:
                                description: name always matches against an exact
                                  label name.
                                type: string
                              type:
                                description: type
                                type: string
                              value:
                                description: value matches against a label value based
                                  on the configured type.
                                type: string
                            type: object
                          type: array
                        severity_conditions:
                          description: severity conditions
                          properties:
                            critical:
                              description: critical
                              properties:
                                conditions:
                                  description: |-
                                    List of conditions to evaluate against a series.
                                    Only one condition -- not all -- needs to match in order to assign a severity to a signal.
                                  items:
                                    description: |-
                                      MonitorCondition monitor condition


                                      swagger:model MonitorCondition
                                    properties:
                                      op:
                                        description: op
                                        type: string
                                      resolve_sustain_secs:
                                        description: Time op operation must evaluate
                                          as false for an active alert, before that
                                          alert resolves.
                                        format: int32
                                        type: integer
                                      sustain_secs:
                                        description: Time op operation must evaluate
                                          to true for the condition to evaluate to
                                          true.
                                        format: int32
                                        type: integer
                                      value:
                                        description: The value to compare to the metric
                                          value using the op operation.
                                        type: number
                                    type: object
                                  type: array
                              type: object
                            warn:
                              description: warn
                              properties:
                                conditions:
                                  description: |-
                                    List of conditions to evaluate against a series.
                                    Only one condition -- not all -- needs to match in order to assign a severity to a signal.
                                  items:
                                    description: |-
                                      MonitorCondition monitor condition


                                      swagger:model MonitorCondition
                                    properties:
                                      op:
                                        description: op
                                        type: string
                                      resolve_sustain_secs:
                                        description: Time op operation must evaluate
                                          as false for an active alert, before that
                                          alert resolves.
                                        format: int32
                                        type: integer
                                      sustain_secs:
                                        description: Time op operation must evaluate
                                          to true for the condition to evaluate to
                                          true.
                                        format: int32
                                        type: integer
                                      value:
                                        description: The value to compare to the metric
                                          value using the op operation.
                                        type: number
                                    type: object
                                  type: array
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              signal_grouping:
                description: signal grouping
                properties:
                  label_names:
                    description: |-
                      Set of labels names used to split series into signals.
                      Each unique combination of labels will result in its own signal.
                      For example, if label_names is ["service", "code"] then all series including labels {service="foo",code="404"}
                      will be grouped together in the same signal.


                      Cannot be used if graphite_query is set.
                    items:
                      type: string
                    type: array
                  signal_per_series:
                    description: |-
                      If this is true, each series will have its own signal.
                      If this is true then label_names cannot be set.
                    type: boolean
                type: object
              slug:
                description: Unique identifier of the Monitor. If slug is not provided,
                  one will be generated based of the name field. Cannot be modified
                  after the Monitor is created.
                type: string
              updated_at:
                description: |-
                  Timestamp of when the Monitor was last updated. Cannot be set by clients.
                  Read Only: true
                type: string
            type: object
          status:
            description: ChronosphereMonitorStatus defines the observed state of ChronosphereMonitor
            properties:
              lastProcessedGeneration:
                format: int64
                type: integer
              message:
                type: string
              state:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
