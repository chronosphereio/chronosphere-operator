---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: chronospherecollections.monitoring.chronosphere.io
spec:
  group: monitoring.chronosphere.io
  names:
    kind: ChronosphereCollection
    listKind: ChronosphereCollectionList
    plural: chronospherecollections
    singular: chronospherecollection
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChronosphereCollection is the Schema for the chronospherecollections
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ChronosphereCollectionSpec defines the desired state of ChronosphereCollection
            properties:
              created_at:
                description: |-
                  Timestamp of when the Collection was created. Cannot be set by clients.
                  Read Only: true
                type: string
              description:
                description: Optional description of the collection.
                type: string
              name:
                description: Required name of the Collection. May be modified after
                  the Collection is created.
                type: string
              notification_policy_slug:
                description: |-
                  Slug of the notification policy used by default for monitors in this collection.
                  This is optional if the collection does not contain monitors or all of its monitors explicitly reference a policy.
                  This does not override the policy used when a monitor explicitly references a policy.
                type: string
              slug:
                description: Unique identifier of the Collection. If slug is not provided,
                  one will be generated based of the name field. Cannot be modified
                  after the Collection is created.
                type: string
              team_slug:
                description: Required slug of the team the collection belongs to.
                type: string
              updated_at:
                description: |-
                  Timestamp of when the Collection was last updated. Cannot be set by clients.
                  Read Only: true
                type: string
            type: object
          status:
            description: ChronosphereCollectionStatus defines the observed state of
              ChronosphereCollection
            properties:
              lastProcessedGeneration:
                format: int64
                type: integer
              message:
                type: string
              state:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
