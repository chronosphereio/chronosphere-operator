---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: chronospheredashboards.monitoring.chronosphere.io
spec:
  group: monitoring.chronosphere.io
  names:
    kind: ChronosphereDashboard
    listKind: ChronosphereDashboardList
    plural: chronospheredashboards
    singular: chronospheredashboard
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChronosphereDashboard is the Schema for the chronospheredashboards
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              Configv1Dashboard configv1 dashboard


              swagger:model configv1Dashboard
            properties:
              collection:
                description: collection
                properties:
                  slug:
                    description: slug
                    type: string
                  type:
                    description: type
                    type: string
                type: object
              collection_slug:
                description: Optional slug of the collection the dashboard belongs
                  to.
                type: string
              created_at:
                description: |-
                  Timestamp of when the Dashboard was created. Cannot be set by clients.
                  Read Only: true
                type: string
              dashboard_json:
                description: Required raw JSON of the dashboard.
                type: string
              name:
                description: Required name of the Dashboard. May be modified after
                  the Dashboard is created.
                type: string
              slug:
                description: Unique identifier of the Dashboard. If slug is not provided,
                  one will be generated based of the name field. Cannot be modified
                  after the Dashboard is created.
                type: string
              updated_at:
                description: |-
                  Timestamp of when the Dashboard was last updated. Cannot be set by clients.
                  Read Only: true
                type: string
            type: object
          status:
            description: ChronosphereDashboardStatus defines the observed state of
              ChronosphereDashboard
            properties:
              lastProcessedGeneration:
                format: int64
                type: integer
              message:
                type: string
              state:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
